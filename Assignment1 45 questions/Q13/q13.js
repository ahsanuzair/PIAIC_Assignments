"use strict";
let transport = ["honda", "car", "jeep"];
for (let i = 0; i < transport.length; i++) {
    console.log("I would like to own a " + transport[i]);
    console.log("the color of " + transport[i] + " would be red \n");
}
